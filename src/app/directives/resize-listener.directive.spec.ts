import { ResizeListenerDirective } from './resize-listener.directive';

describe('ResizeListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeListenerDirective();
    expect(directive).toBeTruthy();
  });
});
